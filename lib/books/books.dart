import 'package:flutter/cupertino.dart';

class books extends StatelessWidget {
  const books({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(
      child: Text("This is Books Page"),
    ));
  }
}
